# 《自由未烬》(Liberty Unquenched) Bug 列表 —— 去重整合版

> 本文档由同一目录下三份 Bug 报告去重、合并而成，编号 B-01 ~ B-18，供与《自由未烬-游戏评测整合报告》互相引用。

## 一、整合说明

**整合来源（3 份原始 Bug 报告）：**

| 来源文件 | 测试入口 | 测试方式 | 覆盖内容 |
|---|---|---|---|
| `BUG_REPORT (2).md` | 1931/1933 剧本 · normal（另测 easy） | headless 驱动真实游戏逻辑（`gameReducer` 等），三条路线多次复跑 | 路线级死链、循环、AI 平衡、经济 |
| `bug_report.md` | 1931 剧本 · 简单模式（1931.4–8，4 回合） | 自动化浏览器 `element.click()` 驱动 React | UI 卡死、牌库、数值、稳定性、可发现性 |
| `Bug汇总.md` | 1931 剧本 · 沙盒模式 | 本地浏览器实际操作 | 回合软锁、拉蒙路线门槛、精度、lint |

**去重原则：**

1. 同一根因 / 同一症状在多个文档重复出现 → 合并为一条（严重度取各来源最高）。
2. 一条新条目内含多个独立实例（不同位置/表现）→ 用子项分别列明，保留原始信息。
3. 纯测试环境问题、工程工具问题、观察项（非 Bug）、待回归项 → 一律归入文末"附注"，不占主表。

**统计：** 原始表格记录 25 条（`BUG_REPORT (2)` 9 条 + `bug_report` 12 条含 BUG-E1 + `Bug汇总` 4 条含 TOOL-001）→ 消除 5 处跨文件重复（合并为 4 组：BUG-01/02、#2+BUG-002、BUG-03+BUG-001、#8+BUG-04+BUG-003）后得 20 条；其中 2 条（BUG-E1、TOOL-001）属环境/工程项归入附注 → **主表 18 条**。附注另含观察项 4 条、待回归项 3 条（均为原文文本内容，非表格行）。

**严重度约定：** 🔴 高（阻断/不可达）· 🟠 中（明显缺陷，有绕行或影响局部）· 🟡 低（观感/数值瑕疵）· ⚪ 信息（潜在风险/流程问题）。

> ⚠️ **跨轮次结论差异（重要）**：headless（normal）轮与浏览器 UI（沙盒/简单）轮对「路线一：避免内战/拉蒙任总统是否可达」得出相反结论（见 B-02、B-03 内"分歧"标注）。两轮测试入口与推进深度不同（headless 推进至 1936-07 之后；UI 沙盒轮止于 1932-09），且可能基于略有差异的代码状态，**该两条需回归裁决**，勿仅凭单一报告定性。

## 二、去重对照表（原编号 → 新编号）

| 来源 | 原编号 | 去重去向 |
|---|---|---|
| `BUG_REPORT (2)` | #1 | → B-02 |
| `BUG_REPORT (2)` | #2 | → B-03（与 `Bug汇总` BUG-002 合并） |
| `BUG_REPORT (2)` | #3 | → B-04 |
| `BUG_REPORT (2)` | #4 | → B-06 |
| `BUG_REPORT (2)` | #5 | → B-09 |
| `BUG_REPORT (2)` | #6 | → B-11 |
| `BUG_REPORT (2)` | #7 | → B-08 |
| `BUG_REPORT (2)` | #8 | → B-13a（与 `bug_report` BUG-04、`Bug汇总` BUG-003 合并） |
| `BUG_REPORT (2)` | #9 | → B-14 |
| `bug_report.md` | BUG-01 | → B-01（与 BUG-02 合并，同源） |
| `bug_report.md` | BUG-02 | → B-01（与 BUG-01 合并，同源） |
| `bug_report.md` | BUG-03 | → B-05a（与 `Bug汇总` BUG-001 合并） |
| `bug_report.md` | BUG-04 | → B-13b |
| `bug_report.md` | BUG-05 | → B-15 |
| `bug_report.md` | BUG-06 | → B-16 |
| `bug_report.md` | BUG-07 | → B-17 |
| `bug_report.md` | BUG-08 | → B-10 |
| `bug_report.md` | BUG-09 | → B-07 |
| `bug_report.md` | BUG-10 | → B-18 |
| `bug_report.md` | BUG-11 | → B-12 |
| `bug_report.md` | BUG-E1 | → 附注 1（测试环境，非游戏缺陷） |
| `Bug汇总.md` | BUG-001 | → B-05b |
| `Bug汇总.md` | BUG-002 | → B-03（与 `BUG_REPORT (2)` #2 合并） |
| `Bug汇总.md` | BUG-003 | → B-13c |
| `Bug汇总.md` | TOOL-001 | → 附注 2（工程工具） |

## 三、Bug 总表

| 编号 | 严重度 | 标题 | 主要来源 |
|---|---|---|---|
| B-01 | 已解决 | 事件/行动界面切换卡死，UI 与 state 脱节（可重复结算） | bug_report BUG-01/02 |
| B-02 | 已解决 | "寂静的共和"不可达：1936-07 内战无条件触发 | BUG_REPORT (2) #1 |
| B-03 | 已解决 | 拉蒙·佛朗哥总统路线：竞选计数死链 + 日志门槛口径不一 | BUG_REPORT (2) #2、Bug汇总 BUG-002 |
| B-04 | 🔴 高 | 阿斯图里亚斯革命胜利路线不可达 | BUG_REPORT (2) #3 |
| B-05 | 🔴 高* | 牌库无合法卡：静默失败（政府牌库）与回合软锁（沙盒自选） | bug_report BUG-03、Bug汇总 BUG-001 |
| B-06 | 🟠 中（偏高） | 选举-解散死循环，大选反复触发 | BUG_REPORT (2) #4 |
| B-07 | 已解决 | 复杂事件稳定性：主线程冻结 + dev server 退出 | bug_report BUG-09 |
| B-08 | 已解决 | 内战地图 AI 难度失衡（normal 碾压 / easy 过弱） | BUG_REPORT (2) #7 |
| B-09 | 已解决 | easy 卡牌子事件选项 effect 丢失，事件无法结算 | BUG_REPORT (2) #5 |
| B-10 | 🟡 低 | easy"返还卡牌"整份快照回滚 | bug_report BUG-08 |
| B-11 | 已解决 | 空选项组件驱动事件可致 reducer 崩溃 | BUG_REPORT (2) #6 |
| B-12 | 已解决 | currentEvent 清空缺统一兜底（潜在卡关） | bug_report BUG-11 |
| B-13 | 已解决 | 数值精度与展示口径（3 实例：浮点脏值/军备小数/跨面板精度） | 三份报告各一例 |
| B-14 | 🟡 低 | 经济死区：预算 -100 钳死、国债破 900、黄金不动 | BUG_REPORT (2) #9 |
| B-15 | 已解决 | 顶栏副标题以模板串字面量渲染 | bug_report BUG-05 |
| B-16 | 🟡 低 | 选举结果文案与计算联盟构成不符 | bug_report BUG-06 |
| B-17 | 已解决 | 事件选项不可用原因提示不足 | bug_report BUG-07 |
| B-18 | 已解决 | 存档入口隐藏、铁人模式静默禁用 | bug_report BUG-10 |

\* B-05 合并了两个严重度不同的实例（沙盒软锁=高、常规牌库静默失败=中），表中取高。

## 四、详细条目

### B-01 事件/行动界面切换卡死，UI 与 state 脱节　🔴 高
- **现象：** 事件选项点击后游戏状态已更新（行动点已扣、卡牌已打出、数值已生效），但旧的事件模态框残留在主区域，新视图（牌库/手牌）永不挂载；界面显示"行动阶段/剩余行动点"，主区域仍是可点击的事件选项。手牌槽残留已打出的卡（含"打出卡牌"按钮），已结算事件仍显示全部选项——**重复点击可造成二次结算**（重复扣资源/重复加数值）。
- **位置/根因：** `src/components/MainArea.tsx` L34–108，AnimatePresence `mode="wait"` 切换未正确卸载/挂载子组件（UI 渲染依赖动画完成，与 state 更新脱节）。与 B-02 为同源问题。
- **影响：** 每回合都可能触发；玩家无法正常出牌，UI 与 state 长期脱节。
- **复现环境：** 浏览器 UI 轮，两次独立复现（事件链路径与卡牌路径各一次）。
- **绕行：** 打开"地图"视图再关闭（强制卸载/重挂载 MainArea）可解除，但每次事件都需一次，体验极差。
- **建议：** 为 AnimatePresence 子组件加稳定 `key` 并避免 `mode="wait"`，或给事件模态加超时兜底渲染；以 `phase`/`currentEvent` 变化直接驱动 UI 判定而非依赖动画完成；结算动作幂等化。
- **关联：** B-12（事件生命周期兜底）、B-07（稳定性）。

### B-02 "寂静的共和"结局不可达：1936-07 内战无条件触发　🔴 高
- **现象：** 和平路线（normal）1936-07 强制弹出 `spanish_civil_war` 超级事件；唯一按钮"拿起武器"把 `civil_war_setup` 置为当前事件，而它的两个选项（默认历史配置/沉浸式事件链）都会将 `civilWarStatus` 置为 `ongoing`。实测唯一一次拿到 `SILENT_REPUBLIC` 结局是 bot 故意不处理超级事件——真实 UI 中超级事件是全屏模态、无法跳过。
- **位置/根因：** `src/game/GameContext.tsx`（NEXT_PHASE、DISMISS_SUPER_EVENT）、`src/game/events/civil_war/civil_war_setup.ts`。1936-07 无条件下发内战超级事件；解散超级事件逻辑没有"避免内战"分支；`civil_war_setup` 无和平选项。与 ending 文案（"政变在引发全面内战前被挫败"）直接矛盾。
- **影响：** "避免内战·政治路线"（路线一）结局死链之一；内容写得很精彩但按推荐路线必撞墙。
- **复现环境：** headless 轮，1931/1933 normal 三条路线多次复跑（置信度：高）。
- **分歧：** 沙盒轮评测（推进至 1932-09）据源码推断"非历史模式可达寂静的共和"，但**未推进至 1936-07 之后**，属推论而非通关实证。两轮结论相反 → **需回归**：以 1931/1933 非历史模式完整推进至 1936-07+ 复验。
- **建议：** 内战触发增加可被避免的条件/分支（政变失败事件、总统权力制衡线、和平选项），或明确说明该结局当前版本不可得。

### B-03 拉蒙·佛朗哥总统路线：竞选计数死链 + 日志门槛口径不一　🔴 高
- **现象（两个互补实例）：**
  - **a. 计数死链（高）：** headless 轮三条路线多局游玩中 `journal_ramon_franco_presidency` 恒为 `inactive`；总统选举初选里"拉蒙·佛朗哥"选项永远灰置（要求 `campaign_count ≥ 3`），总统恒为阿萨尼亚。
  - **b. 门槛口径不一（中）：** "伊比利亚之鹰"日志把"ERC 支持 ≥80"与"完成 3 次总统竞选宣传"同时描述为候选人解锁要求；而候选人选择逻辑只检查竞选宣传次数。函数级验证：ERC 未达 80 时拉蒙仍可进入左翼初选并当选。
- **位置/根因：** `ramon_franco_campaign_count` 仅初始化（`GameContext.tsx:871`），**全代码库无任何事件/卡牌/顾问效果递增它**（grep 核实；`ramon_campaign_event_1/2/3` 效果均不写该字段）→ 3 次宣传门槛永不可满足。`src/game/events/ramon_campaign_events.ts`、`presidential_election_chain.ts`、`journal/ramon_franco_presidency.ts`。
- **影响：** 路线一结局死链之二；玩家可能为并非必需的 ERC-80 白投大量回合，也可能在日志仍显示未完成时（若计数问题修复后）意外发现候选人可选。
- **复现/分歧：** headless 实测灰置（高置信）；沙盒轮认为"逻辑可达"（筹备政党 → PRRevS → 野猪议员链 → 3 次宣传 → 当选），但其试玩止于第三次筹备、未端到端完成 3 次宣传，且函数级验证与 a 的 grep 结论可共存（若计数从不递增，门槛恒不满足）。**需回归。**
- **建议：** 补全 `ramon_franco_campaign_count` 的递增来源（宣传事件/顾问/相关筹备联动）；统一日志与候选人门槛——若 ERC-80 是设计要求则补入门槛，否则从日志移除或改为可选加成。

### B-04 阿斯图里亚斯革命胜利路线不可达　🔴 高
- **现象（三个独立致因）：**
  - ① 1931 剧本：开局 `elections_1931_results` 默认组建共和-社会党联盟（含 PSOE），而 Alianza Obrera 日志的失败判定（PSOE 入盟即永久 failed）在激活判定之前执行 → 日志在可激活前即失败。
  - ② 1933 剧本：1934-08/09 革命自动触发时 Alianza 尚未完成（需 PSOE≥80、进度≥3、工人控制≥55，可用窗口仅 1933-11 → 1934-08），只能选"仅阿斯图里亚斯的同志们参与"→ `asturias_war='failed'`。
  - ③ 阈值矛盾：触发条件（热情>50）低于加入条件（热情>80），逻辑上要求玩家在触发前把热情一次拉过 80。
- **位置/根因：** `src/game/events/asturias_revolution.ts`、`journal/alianza_obrera.ts`、`journal/uhp.ts`、`elections_1931_results.tsx`。Alianza 完成条件与革命触发条件在时间上严重冲突；1933 窗口过窄且随机抽牌难以保证按时凑齐条件。
- **影响：** 路线二（拉拢 PSOE、借阿斯图里亚斯革命提前开战）当前版本实际不可取胜；且 1936-07 内战照常爆发。
- **复现环境：** headless 轮 1933 normal（置信度：高）。
- **建议：** 放宽 Alianza 时间窗口；调整 PSOE 入盟/失败判定的执行顺序；调和革命"触发/加入"阈值（避免 50/80 倒挂）；随机抽牌改为可定向补足关键条件。

### B-05 牌库无合法卡：静默失败（政府牌库）与回合软锁（沙盒自选）　🔴 高（含 🟠 实例）
- **现象（两个实例）：**
  - **a. 抽牌静默失败（中）：** 政府牌库中无满足 `condition` 的卡且弃牌堆无同类卡可回收时，DRAW_CARD 直接 `return state`，点击牌库无任何反馈。实测 1931.5–8 连续多次点击"政府卡牌"毫无效果（政府牌库 7 张卡该时段条件全不满足）→ "政府事务"前期实际完全不可玩。
  - **b. 回合软锁（高）：** 沙盒自选牌库耗尽后：自选面板显示无卡可抽、手中卡"打出卡牌"按钮禁用、"结束回合"按钮不出现 → 回合无法继续。
- **位置/根因：** `src/game/GameContext.tsx`（DRAW_CARD 静默 return；回合结束判定未覆盖"无合法操作"情形）；沙盒自选卡牌 UI。
- **影响：** 玩家不知道牌库何时可用/为何不可用；b 直接阻断流程。
- **绕行（b）：** 进入沙盒工具关闭自选卡牌模式再随机抽牌（会重洗牌库）→ 回合得以继续。
- **建议：** 无合法抽牌时禁用并提示（如"暂无可用政府事务"）；提供至少一种合法出口：自动重洗弃牌堆 / 允许弃牌退牌 / 允许放弃剩余行动点 / 无合法操作时自动显示"结束回合"，并将"结束回合"做成常驻兜底。
- **关联：** B-17（不可用操作提示不足，同一体验家族）。

### B-06 选举-解散死循环　🟠 中（偏高）
- **现象：** 1933 年内 `elections_1933` 触发约 6 次；1934–1936 期间 `elections_1936` 每 1–2 个月触发一次；日志显示"解散议会→大选→组阁→下月再解散"自我循环，事件历史被刷屏；政府在内阁类型间反复横跳（人民阵线/国家阵线）。
- **位置/根因：** `src/game/events/presidential_dissolution.ts`（tags: repeatable）；`src/game/utils/coalition.ts`（checkCoalitionDissolve 凝聚力/席位阈值使新联盟次月即散）；每次解散又安排新选举 → 自我维持循环。
- **影响：** 1933–1936 沦为"每月重选"的机械重复；与 B-14 叠加后经济系统基本失去意义。
- **复现环境：** headless 轮（实测触发频率）。
- **建议：** 解散议会/大选加冷却或一次性标记；调整联盟解散阈值，使新联盟至少有稳定周期。

### B-07 复杂事件稳定性：主线程冻结 + dev server 退出　🟠 中（待诊断）
- **现象：** 游玩至 1931.8"群众集会"事件（11 个选项的复杂事件）时浏览器主线程完全无响应（30s 超时），随后 Vite dev server 进程整体退出（ERR_CONNECTION_REFUSED）。重启后页面与存档（localStorage）恢复，但继续处理该事件可能再次触发。
- **定性：** 未确认归属——应用代码无限循环/死锁或仅为环境内存问题均无法排除。
- **影响：** 复杂事件链可能造成页面冻结并丢失未保存进度（游戏无自动保存）。
- **建议：** 对复杂事件渲染（11 选项及子链）做性能检查，考虑分页/折叠；增加自动存档（每回合写 localStorage），可将崩溃损失降到零。

### B-08 内战地图 AI 难度失衡　🟠 中
- **现象：** normal 三局（含集中兵力、守马德里/巴塞罗那/巴伦西亚、每月征兵整编等应对策略）均在 1937-12 前被打到仅剩 10–25 省（国民军 33–48 省）；AI 每月在战略价值 ≥4 的省份建征兵处并招 1750 人师、以 2 CP 优先吞无防省份 → 滚雪球。easy 的 AI 完全不征兵/不整编/不建设（attackThreshold=1.8）。
- **位置/根因：** `src/map/lib/gameAi.ts`。AI 资源模型（省人力×8/月、工业、征兵处）与"吞噬无防省份"的贪心叠加；难度梯度设计陡峭、无中间档。
- **影响：** 路线三（历史内战）normal 极难取胜；easy 又过弱，形成"前松后紧 + 悬崖"的割裂体验。
- **复现环境：** headless 轮 normal 三局全败。
- **建议：** normal 削征兵/吞地权重；easy 保留基本军事行为（征兵/整编/建设）；增加中间难度档。

### B-09 easy 模式卡牌子事件选项 effect 丢失　🟠 中
- **现象：** easy 打出 `fundraising`/`syndicate_expansion` 后，其 `currentEvent` 的 4 个选项 `effect` 全部为 `undefined`，任何 RESOLVE_EVENT 都无法推进；配合 easy 的"Apply Effect/退还卡牌"包装反复打出同一卡牌形成事件循环、流程卡死。normal 未复现。headless 直接观测证据：`opts=4, types=["undefined"×4]`。
- **位置/根因：** `src/game/GameContext.tsx` PLAY_CARD 的 easy 包装在 `newStateAfterCard.currentEvent` 上追加/重写选项时，effect 的函数引用未正确保留；涉及 `src/game/action_affairs/fundraising.ts`、`syndicate_expansion.ts` 等。
- **影响：** easy 难度下部分行动卡不可用；配合 UI 卡死类问题（B-01）表现更糟。正常 UI 下可能表现为"按钮无效/卡住/无响应"，需按代码路径人工验证。
- **建议：** 修复 easy 包装中的 effect 引用保留；对 easy 下全部生成子事件的卡牌做回归。
- **关联：** B-10（同 easy 出牌包装区）。

### B-10 easy"返还卡牌"整份快照回滚　🟡 低
- **现象：** 简单模式"返还卡牌（Refund costs）"选项的 effect 直接返回 `stateBeforeCard`（整份 state 深拷贝）→ 会把出牌后发生的所有状态变更一并回滚；若玩家在出牌→返还之间插入其他操作，将产生不可预期的状态回退。
- **位置/根因：** `src/game/GameContext.tsx` L2422–2461（PLAY_CARD easy 模式）。设计意图是"免费反悔"，实现粒度却是整份状态快照，属脆弱回滚。
- **建议：** 改为按卡牌记录 delta 做补偿回滚，而不是整份快照。
- **关联：** B-09。

### B-11 空选项组件驱动事件可致 reducer 崩溃　🟠 中
- **现象：** easy 难度进入 `cabinet_formation_1931` → `minister_allocation` 时，任何非组件路径的 RESOLVE_EVENT 都会因 `action.payload is not a function` 抛 TypeError 中断运行。
- **位置/根因：** `minister_allocation`（`src/game/events/elections_1931_results.tsx`，`options: []`）完全依赖 React 组件内 dispatch 结算；事件系统对"组件驱动事件"缺少兜底/保护，也缺少空选项校验（`GameContext.tsx:2374`）。
- **影响：** 崩溃本身高置信；影响面取决于入口（正常 UI 点击路径可能表现为"按钮无效/卡住"）。
- **建议：** 对空选项事件加校验与兜底，RESOLVE_EVENT 对非函数 payload 做保护。
- **关联：** B-12。

### B-12 currentEvent 清空缺乏统一兜底（潜在）　⚪ 信息
- **现象：** `currentEvent` 是否清空依赖 effect 返回值是否显式携带 `currentEvent: null`；若某事件 effect 返回 `{}`（如"让他们吵吧"、"晚点再讨论"），多数路径实测能正确结束，但个别复杂事件（如 1931 选举结果选项）分支多、未见显式兜底，存在低概率"事件不消失"卡关风险（本局未复现）。
- **位置：** `src/game/GameContext.tsx` L2500–2535（RESOLVE_EVENT）。
- **建议：** 由 RESOLVE_EVENT 统一强制置空 `currentEvent`（除非 effect 显式提供新事件），对所有返回 `Partial<GameState>` 的 effect 统一兜底。
- **关联：** B-11、B-01。

### B-13 数值精度与展示口径　🟡 低（3 实例，三份报告各一）
- **a. 张力浮点脏值：** `stats.tension` 实测出现 `60.019999999999996` 等浮点尾数，直接展示不整洁。根因：NEXT_PHASE 张力/经济公式连续浮点累加未做定点处理（部分字段已用 `parseFloat().toFixed()`，张力等没有）。（来源：`BUG_REPORT (2)` #8）
- **b. 军备收入含小数：** 军备 2 → 3.15 → 5.45 → 6.6；回合收入脚本为 `+1`，但实际增量含 0.15/0.3 小数；顶栏显示取整值，与实际状态不符。（来源：`bug_report` BUG-04）
- **c. 同一数值跨面板精度不一：** 顶栏"工人控制"显示 `11.682%`/`33.682%`，左侧危机面板同时显示取整后的 `12%`/`34%`，玩家易误以为两处指标不同。（来源：`Bug汇总` BUG-003）
- **建议：** 数值计算定点化/取整（`Math.round`/定点累加）；展示口径统一为整数或最多一位小数，详细精度放入悬停说明。

### B-14 经济死区　🟡 低
- **现象：** 连年选举/内战后 `budget` 长期卡在 -100（钳制下限）、`public_debt` 涨至 900+、`gold_reserves` 恒为 2200 不动；与 B-06 选举循环叠加后经济基本失去意义。
- **位置/根因：** `src/game/GameContext.tsx` NEXT_PHASE 经济模拟；经济公式与事件循环联动，缺恢复手段与上限提示。
- **建议：** 预算下限外提供恢复事件/手段；国债与黄金加入每月收支联动；数值越界给出提示。

### B-15 顶栏副标题以模板串字面量渲染　🟡 低
- **现象：** 顶栏副标题显示 `` `$CNT-FAI $全国委员会` `` 原样字符，而非拼接后的 "CNT-FAI 全国委员会"（本应为 `${getPartyName(state,'CNT_FAI',isZh,true)}` 的插值结果）。
- **位置：** `src/components/TopBar.tsx` L79（反引号模板字符串被当作字面量渲染）。
- **修复：** 改为 JSX 表达式插值。

### B-16 选举结果文案与计算联盟构成不符　🟡 低
- **现象：** 1931 制宪议会大选结果事件中文文案显示 "PSOE + AR + ERC + PRRS + PRR"，而 `calculateElectionResults` 汇总的是 PSOE + IR + ERC + PRR + UR（AR/PRRS 是历史别名、非当前派系键）→ 玩家无法判断真实联盟，席位数字与标注的政党组合对不上。
- **位置：** `src/game/events/elections_1931_results.tsx` L90-99、L160-168。
- **修复：** 统一使用 `getPartyName` 或与计算键一致的显示名。
- **关联：** 与 B-03 同属"文案与逻辑口径不一致"家族，但系统不同，故单列。

### B-17 事件选项不可用原因提示不足　🟡 低
- **现象：** 不满足条件的选项仅靠 `disabled` + hover 提示（有时显示函数文本）；easy 模式效果预览只在 hover 出现，移动端/无鼠标场景不可见；"无政府主义者不需要虚伪的交易"等无损最后一档与有条件选项混排，玩家难以区分，易误以为游戏卡死。
- **位置：** `src/components/MainArea.tsx` L152–189 / 事件系统。
- **建议：** 常显"条件未满足"文案（含缺什么），并对可选/不可选选项排序分组。
- **关联：** B-05。

### B-18 存档入口隐藏、铁人模式静默禁用　⚪ 信息
- **现象：** 顶栏没有可见"保存"按钮（TopBar 已 import Save/Download 图标但未渲染）；保存/读取入口藏于"设置"面板；hard/historical 模式下 `handleSave` 直接 return、无任何禁用提示。
- **位置：** `src/components/TopBar.tsx` / 存档系统。
- **影响：** 玩家可能找不到存档入口；铁人模式无"已禁用存档"文案。
- **建议：** 保存入口提升至顶栏或设置面板明显位置；禁用时显示提示；配合自动存档（见附注 3）。

## 五、附注（非主表）

**1. 测试环境问题（非游戏缺陷）：** `bug_report` BUG-E1 —— 自动化浏览器下 CDP 坐标点击与键盘事件完全未到达页面（页面级 capture 监听 0 次），仅 `element.click()`（真实 DOM 事件）可驱动 React；疑似窗口焦点/视口缩放（devicePixelRatio=1.5）的坐标映射问题。仅影响自动化，对真实玩家无影响。

**2. 工程工具问题：** `Bug汇总` TOOL-001 —— `npm run lint` 在 TypeScript 检查（`tsc --noEmit`）通过后，因缺少 `scripts/audit-coalition-authority.mjs` 而失败。不影响直接游玩，但阻断标准验证流程与自动化检查。

**3. 观察项 / 改进建议（非 Bug）：**
- **无自动存档：** 仅设置面板可手动存档，刷新即丢失进度（铁人模式更严格）；长线政治模拟建议至少回合结束自动写档（崩溃后 localStorage 存档仍可用，收益大）。
- **效果预览仅限 easy/sandbox：** `showEffectPreview = difficulty === 'easy' || 'sandbox'`，标准/困难模式玩家预判卡牌效果只能靠经验。
- **事件选项文案可为函数：** 如"1931 年大选结果"的 `textZh` 是 `(state) => ...`；对 UI 透明，但直接读取事件对象的外部工具会踩坑（曾出现 `o.textZh.slice is not a function`）。
- **沙盒"触发结局"按钮** 适合检查结局展示，不应替代正常路线通关验证。

**4. 待回归项（来自 `Bug汇总`，需完整 UI 回归）：**
- 1933、1936 开局不应再次弹出"西班牙长枪党成立"。
- CNT 第四次代表大会应在 1936-05 触发，而非在 1936-07 剧本开局补触发。
- 1936 内战地图共和国胜利、1934 阿斯图里亚斯提前内战胜利的完整 UI 通关回归。

**5. 总体备注：** 除上述问题外，政治（选举/议会/联盟）、卡牌、顾问、日志、地图战斗等主要系统在 headless 轮（normal）均能正常运转，未见崩溃性全局错误；normal 难度主流程整体可玩。B-09/B-11/B-12 的正常 UI 表现可能为"按钮无效/卡住/无响应"，建议按代码路径进一步人工验证。

---

*本文档为去重整合产物：原始文件 `BUG_REPORT (2).md`、`bug_report.md`、`Bug汇总.md` 保留在工作区，可作为证据与补充背景引用。配套评测见《自由未烬-游戏评测整合报告.md》。*
